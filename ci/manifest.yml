projects:
  - libvirt-php
  - libvirt+dist

gitlab:
  namespace: libvirt
  project: libvirt-php

targets:
  debian-10:
    projects:
      - libvirt-php
      - libvirt+minimal

    jobs:
      - arch: x86_64
        template: .native_git_build_job
        suffix: -git

  debian-sid:
    jobs:
      - arch: x86_64
        allow-failure: true

  fedora-35: x86_64

  fedora-rawhide:
    jobs:
      - arch: x86_64
        allow-failure: true

  opensuse-tumbleweed:
    jobs:
      - arch: x86_64
        allow-failure: true

  ubuntu-2004: x86_64

  ubuntu-2204: x86_64
